# Date format
date-format: "MMMM YYYY"

# Default for table of contents (design has none on detail pages)
toc: false

include-back-link: true

# Default knitr options
execute:
  echo: false
  message: false
  warning: false
